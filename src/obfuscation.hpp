#ifndef OBFUSCATION_HPP
#define OBFUSCATION_HPP

#include <string>

// Funcția care primește un payload și returnează versiunea ofuscată în format hex
std::string obfuscate_payload(const std::string& payload);

#endif // OBFUSCATION_HPP
